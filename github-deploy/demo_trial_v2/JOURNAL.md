# demo_trial_v2 journal (SIM2 agent, 2026-09-08)

Owner instruction (verbatim, 2026-09-08): "i wanted you to fix the simulated trial to not be circular."
Design: see make_demo_trial_v2.py docstring and README_demo_v2.md. Sessions cached under sessions/, never deleted.

- Smoke test: one persona session (cell_culture, mid traits) ran 6 turns in 4.5 min sequential; composite 0.62; persona challenged the planted error via the figure checker. Copy: scratchpad smoke_session.json.
- Decision: re-asks capped at 1 per topic (smoke run burned 2 turns re-asking for a non-existent record).
- Decision: transcripts.jsonl holds in-trial LLM-arm sessions only (idx 0,1); pre-test sessions (idx -1, both arms) go to pretest_transcripts.jsonl and transcripts_with_pretest.jsonl, because analyze_trial.py (as of this run) averages ALL of a participant's sessions into intrial_composite and takes session -1 as "first", which would put the pre-test transcript on both sides of the predictive-validity correlation. Patch proposed in analyze_trial_patch.diff (not applied).
- 10:15 NEGATIVE RESULT preserved: first 30-odd sessions (sessions_v0_verify_saturated/, with run_main.log) had verification 0.94: per-turn check probability = verify trait compounded over ~5 turns, and the HAIKU checker finds the planted error whenever it looks; 2 itemised submissions also flagged the error with no chat challenge (submission prompt invited arithmetic). Fix: one check per session at a per-turn hazard h = 1-(1-verify)^(1/(n_turns-1)) so P(check) = verify trait; itemised submission prompt now forbids fresh arithmetic. Main run restarted from zero est sessions (ceilings kept: expert persona has verify 0.97 either way).
- 2026-09-08 11:18 README_demo_v2.md written by a fresh agent after analyze_trial.py ran (11:15, unpatched; analyze_trial_patch.diff NOT applied, analyze_trial.py held by another session). Hash check: 48 in-trial + 48 pre-test + 12 ceiling records all distinct, 0 message-level overlaps. Stale v1 caveat sentences in results/trial_report.md listed there (lines 17, 18, 32, 45, 75). Patch reads truth.json circularity_note/ceiling_note keys that make_demo_trial_v2.py does not write. Numbers transcribed, not re-derived; verifier V26 pending.
